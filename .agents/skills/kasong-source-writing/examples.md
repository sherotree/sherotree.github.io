# 结构模板与写法示例

以下只学**结构与节奏**。括号内换成你自己的题材；不要复用 React技术揭秘 原文案例与类比。

---

## A. 理念章（问题 → 瓶颈 → 解法）

**标题**：`{框架} 理念` / `{能力}要解决什么`

**骨架**

```markdown
软件的设计是为了服务理念。只有懂了设计理念，才能明白为了实现这样的理念需要如何架构。

所以，在我们深入源码架构之前，先来聊聊`{框架}`理念。

## {框架} 理念

我们可以从[官网/公开材料]看到核心理念：{一句话}。

可见，关键是实现`{关键词}`。那么制约`{关键词}`的因素是什么呢？

日常使用中，有两类场景会制约`{关键词}`：

{场景 A → 概括为瓶颈 1}

{场景 B → 概括为瓶颈 2}

这两类可以概括为：

- {瓶颈 1}
- {瓶颈 2}

`{框架}`是如何解决这两个瓶颈的呢？

## {瓶颈 1}

{用极简 Demo 复现卡顿/失败}

{用一帧时间 / 线程模型等基础知识解释「为什么会卡」}

如何解决这个问题呢？

答案是：{一句话解法}。

{机制名}的关键是：{更底层的一句话}。

## {瓶颈 2}

{网络/异步等客观限制先承认}

{用人机交互或产品对比说明「感知」而非「消灭延迟」}

为此，`{框架}`实现了{能力}；在源码内部，同样需要{与瓶颈 1 共通的底层能力}。

## 总结

通过以上内容，我们可以看到，`{框架}`为践行「{理念}」做出的努力。

关键是解决{瓶颈 1}与{瓶颈 2}。落实到实现上，则需要{一句话架构结论}。

## 参考资料

- {官方文档 / Conf 演讲}
```

---

## B. 概念多层定义章（一词三义）

**标题**：`{概念}的实现原理` / `{概念}的含义`

**骨架**

```markdown
在上一节中，我们提到的{口语名}在`{框架}`中有个正式称呼——`{术语}`。

接下来让我们了解下`{术语}`因何而来？他的作用是什么？

## {术语}的起源

最早的官方解释来源于{年份 + 材料}。

从上一章我们知道：

{旧实现的限制 → 卡顿/不可中断…}

为了解决这个问题，`{新版本}`将{旧模型}重构为{新模型}。曾经的数据结构无法满足需要，于是`{术语}`应运而生。

## {术语}的含义

`{术语}`包含三层含义：

1. **作为架构**：{对比旧架构命名}
2. **作为静态数据结构**：{保存哪些信息}
3. **作为动态工作单元**：{保存哪些「本次更新」信息}

## {术语}的结构

属性很多，但我们可以按三层含义分类来看。

```js
function Node(/* ... */) {
  // 作为静态数据结构
  // ...

  // 用于连接成树
  // ...

  // 作为动态工作单元
  // ...
}
```

### 作为架构来说

{child / sibling / return 等连接方式 + 小例子}

### 作为静态的数据结构

{字段列表 + 一行注释}

### 作为动态的工作单元

{字段列表}；调度相关字段会在讲解`{模块}`时介绍。

注意

{版本重构 / 字段更名 / 读者源码过旧时的提示}

## 总结

本节我们了解了`{术语}`的起源与结构。那么`{术语树}`和页面上的`{真实树}`有什么关系？我们会在下一节讲解。

## 参考资料

- {Conf 卡通演讲 / 深度文章}
```

---

## C. 阶段流程章（递 / 归 / 双缓存）

**标题**：`流程概览` / `什么是「双缓存」`

**骨架**

```markdown
本章我们会讲解`{节点}`是如何被创建并构建`{树}`的。

`{阶段}`开始于`{入口A}`或`{入口B}`的调用。这取决于本次更新是同步还是异步。

我们现在还不需要学习这两个方法，只需要知道其中会调用：

```js
// 同步：不让出
// 并发：shouldYield 时中止
```

可以看到，唯一区别是是否调用`shouldYield`。

## 「递」阶段

从根向下深度优先；对每个节点调用`{begin}`，创建子节点并连接。

到叶子后进入「归」阶段。

## 「归」阶段

调用`{complete}`。有兄弟则进入兄弟的「递」，否则进入父级的「归」。交错直到回到根。

## 例子

```js
// 极简组件
```

`{阶段}`会依次执行：

```text
1. root begin
2. ...
n. root complete
```

注意

{特殊优化导致「某子节点没有 begin/complete」的原因}

若将真实实现简化为递归版，大体如下：

```js
function walk(node) {
  // begin
  if (node.child) walk(node.child);
  // complete
  if (node.sibling) walk(node.sibling);
}
```

## 总结

本节介绍了`{阶段}`会调用的方法。接下来两节分别讲解`{begin}`与`{complete}`做的具体工作。
```

---

## D. 函数精读章（从传参看执行）

**标题**：`{函数名}` / `方法概览`

**骨架**

```markdown
上一节我们了解到`{阶段}`可分为「递」与「归」。这一节看看「递」阶段的`{fn}`究竟做了什么。

## 方法概览

可以从[源码锚点]看到定义。整个方法大约有 N 行。

我们已经知道，`{fn}`的工作是：传入`当前节点`，创建`子节点`。我们从传参来看具体如何做。

### 从传参看方法执行

```js
function fn(current, workInProgress, lanes) {
  // ...省略函数体
}
```

其中传参：

- `current`：{语义}
- `workInProgress`：{语义}
- `lanes`：{延后讲}

所以我们可以通过`current === null ?`区分`mount`还是`update`。

基于此，`{fn}`的工作分两部分：

1. **update**：满足条件时可复用…
2. **mount**：按`tag`创建不同类型子节点…

## update 时

满足如下情况可走优化路径：

1. {条件}
2. {条件}

## mount 时

不满足优化路径则新建子节点；常见类型最终进入`{reconcile}`。

## {核心子函数}

从函数名可以看出这是`{模块}`的核心。那么他究竟做了什么呢？

- mount：…
- update：…（俗称 Diff）

注意

{两个几乎相同的函数，唯一区别是…} —— 用思想实验解释「为什么要这样」

## 参考资料

- `{fn}`流程图
```

---

## E. 算法章（限制 → 分类 → 分节）

**标题**：`概览` / `单节点 Diff` / `多节点 Diff`

**骨架**

```markdown
本章为选读章节（若适用）。

在`{上游章}`我们提到：update 时会将当前输出与上次`{节点}`比较，生成新节点。

为了防止概念混淆，这里再强调：

某一时刻最多有 N 个对象与同一`{真实节点}`相关：

1. …
2. …
3. …
4. …

`{算法}`的本质是对比{A}与{B}，生成{C}。

## 瓶颈以及框架如何应对

完全比对复杂度过高 → 预设 K 条限制：

1. …
2. …
3. …

## 如何实现

从入口`{entry}`出发，按`newChild`类型分流。

可按同级数量分为两类：

- 单节点：…
- 多节点：…

接下来两节分别讨论。
```

章末可加**小习题**（key/type 变化能否复用），强迫读者用限制条件推理，而不是背结论。

---

## 句式速查（节奏，非金句库）

承接：`通过上一节的学习，我们了解了…`  
设问：`那么制约…的因素是什么呢？` / `他究竟做了什么呢？`  
收束：`基于此原因，…可以分为两部分`  
延后：`会在讲解{模块}时再介绍`  
注意：`值得一提的是…` / `答案十分巧妙：…`  
钩子：`那么…呢？我们会在下一节讲解。`
